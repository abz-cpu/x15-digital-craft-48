/** Screen pixels per world millimetre at 100% zoom. */
export const BASE_PX_PER_MM = 0.06;

/** Snap grid, mm. */
export const GRID_MM = 100;

/** Visible dot-grid spacing, mm. */
export const DISPLAY_GRID_MM = 500;

/** Endpoint-snap radius in screen pixels (converted to mm at current zoom). */
export const ENDPOINT_TOLERANCE_PX = 14;

/** Smallest room edge the room tool will create, mm. */
export const MIN_ROOM_MM = 500;

export const ZOOM_MIN = 0.15;
export const ZOOM_MAX = 4;
export const ZOOM_STEP = 1.15;
